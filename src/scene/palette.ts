/**
 * One palette, shared by the shader uniforms and the CSS custom properties in
 * global.css. Indigo base, mint accent, amber heat — deliberately not the
 * default cyan/magenta cyberpunk pairing.
 */
export const PALETTE = {
  bg: '#05060c',
  ink: '#5a6bc4',
  accent: '#5ef0cf',
  hot: '#ff9a6b',
} as const
